#include <cstdio>
#include <iostream>
#include <vector>

using namespace std;

int blocos[26];
vector<int> minBlocos;
int t, n, m, b;

int min(z) {
    int menor = 0;
    for (int i = 0; i < n; i++) {
        if ( 1+minBlocos[z-1]  )
    }
}

int main() {
    scanf("%d", &t);

    while(t--) {
        scanf("%d", &n);

        for (int i = 0; i < n; i++) {
            scanf("%d", &blocos[i]);
        }

        qsort(minblocos

        minBlocos.clear();
        minBlocos.push_back(0);
        for (int i = 1; i < m; i++) {
            minBlocos.push_bacK(min(i));
        }
    }

}
